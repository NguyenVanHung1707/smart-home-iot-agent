# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0
python-dotenv>=1.0.0
PyJWT>=2.10.0

# AI Agent / local OpenAI-compatible llama.cpp endpoint
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# MQTT device command, state, and acknowledgment transport
paho-mqtt>=2.1.0

# Voice pipeline
# - python-multipart receives browser WAV uploads.
# - sherpa-onnx runs Vietnamese Zipformer STT.
# - httpx calls the Piper TTS service over the Compose network.
# Frontend/backend energy VAD uses existing TypeScript/Python code and needs no extra package.
python-multipart>=0.0.20
sherpa-onnx==1.13.4
httpx>=0.28.0

# Database (uncomment as needed)
# sqlalchemy>=2.0.0
# alembic>=1.14.0
# psycopg2-binary>=2.9.0

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Dev tools
ruff>=0.8.0
mypy>=1.13.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
